{
  "name": "@private-event-tickets/tests",
  "version": "0.1.0",
  "private": true,
  "description": "Simulation-level tests for the event-tickets Compact smart contract",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.1.0",
    "vitest": "^3.1.0"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-runtime": "^0.15.0"
  }
}
