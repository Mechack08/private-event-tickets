# Hoist all packages to the root node_modules to keep imports working
# across workspace packages.
shamefully-hoist=true

# Strict peer-dependency resolution — warns but does not fail on mismatches,
# useful while the Midnight SDK ecosystem is still evolving.
strict-peer-dependencies=false
auto-install-peers=true
